<h1>Visite du produit :
    {{ product.name }}</h1>

<h2>Details du produit :</h2>

<ul>
    <li>Prix:
        {{product.price}}
        &euro;
    </li>
    <li>Slug:
        {{ product.slug}}</li>
</ul>
